generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL="mysql://root:Morango10@15@@localhost:3306/risktrack"")
}

model User {
  id        Int     @id @default(autoincrement())
  email     String  @unique
  password  String
}
